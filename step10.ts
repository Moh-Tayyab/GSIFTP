const names1 : string[] = ["Tayyab" , "Tariq" , "Talha" , "Tahir" , "Taha"]

const greetingMessage : string = 'Hey'

for (let i = 0;  i < names1.length; i++ ) {
    console.log(`${greetingMessage}  ${names1[i]}!`);
}